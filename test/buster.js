var config = module.exports;

config["Couchtato Tests"] = {
    rootPath: "../",
    environment: "node", // or "node"
    sources: [
        "lib/*.js"
    ],
    tests: [
        "test/*.js"
    ]
}
